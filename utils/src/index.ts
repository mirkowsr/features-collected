// Add shared hooks, helpers, types, and middlewares here.
// Everything exported from this file is available in all features via @repo/utils.

const testUtil = () => {
  console.log('This is a test utility function.')
}

export { testUtil }
